# 픽셀렐름(가제) 기획 문서

> 작업 타이틀: **PixelRealm** (가칭, 나중에 바꿔도 됨)
> 장르: 도트 탑뷰 액션 RPG, 라이트 MMO
> 상태: 초안 v0.1 — 같이 다듬는 중

## 1. 한 줄 컨셉

도트 그래픽 탑뷰 화면에서, 여러 명이 같은 존(맵)에 접속해 실시간으로 몬스터를 사냥하고 캐릭터를 키우는 라이트 MMO 액션 RPG.

## 2. 확정된 방향

| 항목 | 결정 |
|---|---|
| 핵심 장르 | 액션 전투 + 성장 (라이트 MMO) |
| 전투 방식 | 실시간 액션 — 방향키 이동 + 마우스/방향 조준 + 쿨타임 스킬 |
| 월드 구조 | 여러 개의 맵(존/방)으로 분리, 존마다 서버 접속 |
| 아트 스타일 | 도트(픽셀아트) 탑뷰 |
| 기술 스택 | 클라이언트: 바닐라 JS + Canvas / 실시간 존 서버: Node.js + WebSocket(ws) / 영속 데이터: Supabase |
| 프로젝트 위치 | 머니로드와 완전히 분리된 새 프로젝트 (`pixelrealm/`) |

## 3. 핵심 루프

```
존 입장 → 몬스터 사냥(실시간 액션 전투) → 경험치·아이템 드랍
   → 레벨업 / 장비 강화 → 더 강한 존·몬스터 도전 → 반복
   (+ 마을에서 상점/창고/파티 구성)
```

세션 하나가 짧게 끝나도 성장이 남는 것이 핵심 — 접속할 때마다 "조금 더 강해진다"는 느낌을 주는 게 목표.

## 4. 전투 시스템 — 실시간 액션 (메이플식, 변경됨 v0.3)

> v0.1(실시간 액션) → v0.2(포켓몬식 턴제 인카운터)로 갔다가, 직접 플레이해보니 턴제가 재미없어서
> **메이플스토리식 실시간 전투**로 다시 돌아왔다. 화면 전환·메뉴 없이, 필드 위에서 바로 때리고 맞는다.

- 필드: 몬스터가 맵 위를 실시간으로 돌아다니고(방황 AI), 항상 이름 없이 체력바만 머리 위에 떠 있음
- 공격: 클릭 또는 스페이스바 — 내 캐릭터가 바라보는 방향(마우스 조준) 앞의 부채꼴 범위(사거리 50, 좌우 약 60도)에 있는 몬스터를 즉시 타격
  - 쿨타임 450ms, 서버가 사거리·각도·쿨타임을 전부 권위로 검증
  - 맞은 몬스터는 그 즉시 도트 데미지 숫자(예: `-5`)가 떠오르고 체력바가 줄어듦, 처치 시 `+10 EXP` 텍스트
  - 화면 전환 없음 — 여러 몬스터를 동시에 상대할 수 있고, 다른 플레이어도 같은 화면에서 그 전투를 그대로 봄(협동 사냥 가능)
- 몬스터 AI: 평소엔 방황, 플레이어가 일정 거리(140) 안에 들어오면 그 플레이어를 추적(어그로), 근접하면 자기 쿨타임(900ms)으로 반격. 플레이어가 멀어지면(220 이상) 다시 방황으로 복귀
- 피격: 내 캐릭터 위에도 데미지 숫자가 붉은색으로 뜸. HP 0이 되면 광장 스폰 지점으로 귀환 + 전량 회복(死亡 페널티 아직 없음)
- 몬스터 처치 시 일정 시간(6초) 후 다른 위치에 재스폰

**결정 필요**:
- 기본 공격 하나뿐인데, 쿨타임 스킬(2번째 액션)을 언제 추가할지
- 넉백/히트스턴처럼 손맛을 더할 타격감 요소를 넣을지
- 직업/클래스 구분 여부 (지금은 모든 플레이어가 공격력 동일한 단일 스탯)
- 경험치·레벨업을 실제로 저장할지(Supabase 연동 전까지는 접속마다 초기화됨)

## 5. 성장 시스템 — 레벨업 확정 (v0.4)

- 시작 스탯(레벨 1): HP 30 / ATK 6 (지금 코드 기본값과 동일)
- 필요 경험치 공식: `requiredExp(level) = 20 + (level - 1) * 15`
  - 1→2: 20, 2→3: 35, 3→4: 50 ... 갈수록 조금씩 더 필요
- 레벨업 보상: `maxHp += 6`, `atk += 1.5`(반올림), 레벨업 순간 HP 전량 회복
- 레벨 상한은 엔진상 없음 — 월드 1 기준 권장 구간은 레벨 1~10
- 장비/강화/드랍 아이템은 이번 범위에서 제외, 나중 단계로 미룸(아래 항목 유지):
  - 장비: 무기/방어구 슬롯, 등급(일반→고급→희귀→영웅 식), 드랍 기반 획득
  - 강화: 장비 강화(성공/실패 확률)
  - 골드/재화: 몬스터 드랍 + 퀘스트 보상, 마을 상점에서 소비

## 6. 월드 구조 (초안 제안)

존 단위로 서버 룸을 나누는 구조라, 초기엔 아래 정도로 시작하는 걸 추천:

1. **마을 (세이프존)**: 상점, 창고, 파티 모집, PvP 불가, 몬스터 없음
2. **필드1 (초급 사냥터)**: 약한 몬스터, 튜토리얼 겸 사냥터
3. **필드2 (중급 사냥터)**: 필드1보다 강한 몬스터, 파티 권장
4. **던전 (인스턴스, 나중 확장)**: 파티 단위로 별도 룸 생성, 보스 포함

각 존 = 서버에서 별도 room(WebSocket 룸)으로 관리 → 존 간 이동 시 재접속/핸드오프.

## 6-1. 월드 1 상세 기획 (v0.6 — 오픈월드로 전환)

첫 콘텐츠 구역의 이름(가제): **"초록빛 시작 마을"**.
비주얼(스프라이트/타일)은 맨 마지막 단계로 미루고, 지금은 도형 placeholder 그대로 유지 — 이번 기획은 구조·수치·시스템에 집중.

> v0.4에서는 마을/필드1/필드2를 포탈로 순간이동하는 별도의 방(room) 3개로 만들었다가,
> **진짜 2D 오픈월드**로 다시 설계했다 — 자세한 맵 구조·좌표·미니맵은 6-3 참고.
> 레이드만 예전처럼 별도의 인스턴스(room)로 분리되어 있다.

### 지역 구성 (대도시를 중심으로 사방에 배치)
1. **대도시**(중앙, 세이프존, 기본 스폰) — 몬스터 없음, 분수 + 레이드 접수처 2곳
2. **필드1 "들꽃 초원"**(북쪽) — 초급, 권장 레벨 1~4, 지역 안에 작은 마을 있음
3. **필드2 "황혼 언덕"**(동쪽) — 중급, 권장 레벨 5~10, 지역 안에 작은 마을 있음

지역 사이에 벽이나 로딩 없이 그냥 걸어서 넘어감. 화면 하단 HUD의 "구역" 표시가 현재 위치에 따라 실시간으로 바뀜.

### 몬스터 로스터
| 몬스터 | 등장 존 | HP | ATK | EXP | 비고 |
|---|---|---|---|---|---|
| 슬라임 | 필드1 | 18 | 3 | 10 | 기존 그대로, 기본 몹 |
| 버섯 | 필드1 | 14 | 4 | 12 | 슬라임보다 더 아프게 때리지만 더 빨리 죽는 견제형 |
| 들개 | 필드2 | 30 | 6 | 25 | 추적 속도(90)·어그로 범위(180)가 슬라임(70/140)보다 큼 — 필드2의 메인 위협 |

필드2에는 들개 위주로 배치하되 슬라임도 소수 섞어서 난이도 완충(전부 들개면 너무 빡셀 수 있음).
셋 다 지금처럼 도형+색만 다르게 구분(예: 슬라임=초록, 버섯=주황 계열, 들개=회색 계열) — 정식 스프라이트는 나중에.

### 남은 미결 사항 (구현하면서 정하기)
- 필드1→필드2 포탈을 마을에 둘 다 놓을지, 필드1을 통과해야 필드2로 가게 할지 (지금은 "둘 다 마을에서 직행 가능"으로 확정했지만 밸런스 보면서 바뀔 수 있음)
- 존 이동 시 캐릭터 스탯(레벨업 반영된 HP/ATK)이 자연스럽게 유지되는지 — 서버가 zone과 무관하게 player 오브젝트 하나를 계속 들고 있으면 되므로 구조적으로는 문제 없음

## 6-2. 보스 레이드 시스템 (v0.5, 구현 완료)

마을을 제외한 필드(필드1/필드2)마다 보스 레이드가 하나씩 있다.

### 입장 방법
- 각 필드 중앙에 "레이드 입구"(보라색 원형 구역)가 있음. 걸어 들어가면 **혼자 도전 / 파티 모집 / 취소** 선택지가 뜸
- **혼자 도전**: 그 즉시 나만 들어가는 전용 레이드 인스턴스가 만들어지고 바로 입장
- **파티 모집**: 레이드 방이 하나 생성되고(호스트=나) 바로 입장해서 싸울 수 있음. 다른 유저는 **마을에 있는 해당 필드용 레이드 접수처**(포탈)에서 현재 열려있는 파티 방 목록(호스트 이름·인원·보스 HP%)을 보고 참가할 수 있음 — 드롭인 co-op, 최대 4인
- 레이드 중 언제든 "레이드 나가기" 버튼으로 마을로 복귀 가능. 레이드 중 죽으면 자동으로 마을로 돌아감(일반 필드에서 죽으면 그 필드 안에서만 리스폰되는 것과 다름)
- 보스를 잡으면 레이드 참가자 전원에게 경험치 지급 → 3초 뒤 전원 마을로 자동 이동, 레이드 인스턴스 삭제

### 보스 두 종류 (필드마다 패턴이 다름)
| 보스 | 등장 | 약점 속성 | 특수 패턴 |
|---|---|---|---|
| 얼음 슬라임 킹 | 필드1 레이드 | 화속성 | 5초마다 0.9초 예열(정지) 후 **냉기 파동**(주변 100px AoE 데미지 + 2.5초 이동속도 절반 슬로우) |
| 불꽃 들개 대장 | 필드2 레이드 | 빙속성 | 4.5초마다 0.6초 조준(예열) 후 **돌진**(빠른 직선 대시로 경로상 전원 타격) → 0.8초 무방비 회복 시간 |

특수 패턴 예열 중에는 보스 주변에 빨간 경고 링이 펄스로 표시됨(때릴 타이밍/피할 타이밍을 알려줌).

### 속성 시스템 (장비 시스템 나오기 전 임시 버전)
- 아직 무기/장비가 없어서, 숫자 `1`(무속성) `2`(화속성) `3`(빙속성) 키로 즉시 내 공격 속성을 바꿀 수 있게 해둠(HUD에 표시)
- 보스의 약점 속성으로 때리면 데미지 1.5배. 일반 몬스터에게는 속성 보너스 없음
- 나중에 실제 무기 아이템이 생기면 "무기에 속성이 붙는" 방식으로 자연스럽게 교체 가능하도록 설계

### 구현 중 잡은 버그
- 레이드 입구를 분수처럼 "부딪히면 밀려나는" 충돌체로 만들었더니, 플레이어가 물리적으로 입구 반경 안에 절대 들어갈 수 없어서 레이드 자체가 시작이 안 되는 문제가 있었음 → 레이드 입구는 충돌 없이 통과 가능하게 수정(마을 분수만 충돌체로 유지)

## 6-3. 오픈월드 전환 (v0.7 — 진짜 2D 오픈월드)

> v0.6에서는 지역 3개를 가로 일렬로 붙인 "긴 복도"에 가까웠다. 실제 지역처럼 사방으로 넓게 펼쳐진
> 진짜 2D 오픈월드로 다시 만들었다.

### 맵 구조 — 대도시를 중심으로 사방에 지역 배치
- 전체 맵 3200×3200. **대도시(수도)** 가 지도 중앙(600×600)에 있고, 신규 접속 시 항상 대도시에서 시작
- **들꽃 초원**(필드1)은 대도시 **북쪽**, **황혼 언덕**(필드2)은 **동쪽** — 가로 일렬이 아니라 실제로 다른 방향에 있어서 위/오른쪽 등 자유롭게 걸어서 탐험함
- 지역 사이엔 벽이나 로딩 없이 그냥 걸어서 넘어감. 경계가 아닌 곳(지역 밖 공터)은 "황야"로 취급 — 안전하지만 딱히 콘텐츠는 없는 여백
- 레이드는 여전히 별도의 작은 인스턴스 room(800×600)으로 분리 — 오픈월드 좌표와 무관
- 필드 사망 시엔 대도시 중앙으로 귀환

> v0.8에서 지역마다 있던 작은 마을은 없앴고, 대신 **대도시 하나 + 변방 도시 하나** 구조로 바뀌었다. 자세한 건 6-4 참고.

### 카메라 / 미니맵 / 전체 지도
- **카메라는 절대 멈추지 않고 캐릭터를 항상 화면 정중앙에 둠** — 맵 가장자리에 가도 카메라가 안 밀리고, 대신 맵 바깥은 그냥 빈 여백(보이드)으로 보임
- 화면 우상단에 정사각형 미니맵 상시 표시: 지역마다 색으로 구분, 분수·레이드 입구 위치, 내 위치, 현재 카메라 범위(흰 테두리)
- **M키**로 화면 중앙에 큰 전체 지도 오버레이(지역 이름 라벨 포함). 다시 M을 누르면 닫힘
- 바닥 타일도 지역별로 다르게 틴트

## 6-4. 지역 확장 + 경제 시스템 (v0.8, 구현 완료)

### 월드 재구성 — 대도시 + 변방 도시 + 지역 4개
- 지역마다 있던 작은 마을은 삭제. 대신 대도시 반대편(남서쪽)에 **변방 도시**를 새로 만듦
- 전체 맵을 4800×4800으로 키우고, 지역을 2개 추가해서 총 4개(이후 v0.9에서 3개 더 추가됨 — 6-5 참고)

### 경제 시스템 — 상점 / 대장간 / 인벤토리 / 무기 내구도 (v0.9에서 골드 획득 방식 변경됨, 아래는 이후 내용과 함께 6-5 참고)
- **무기**: 처음엔 "낡은 검"(공격력+0, 내구도 30/30, 무속성) 장착. 공격할 때마다 내구도 1씩 감소, 0이 되면 데미지가 절반으로 줄어듦(수리 전까지 페널티)
- **인벤토리**: 보유 아이템 목록. 무기는 "장착" 버튼으로 교체(기존 무기는 가방으로), 물약은 "사용" 버튼으로 즉시 소비
- **상점**: 화염검/빙결검(공격력+2, 속성 부여, 80G) · 강철검(공격력+4, 무속성, 120G) · 체력 물약(HP+20, 15G) 판매. **속성이 있는 무기를 장착하면 그 속성으로 공격** — 숫자 키로 속성을 직접 고르던 방식은 없어지고, 장비에서 자연스럽게 나오는 방식으로 교체됨(레이드 보스 약점 공략과 연결)
- **대장간**: 장착 중인 무기를 강화(공격력+1, 비용은 강화할수록 증가: 30G→50G→70G...)하거나 내구도를 전액 수리(부족한 내구도 1당 2G)
- 상점/대장간/레이드 접수처는 전부 "들어가면 자동으로 창이 뜨는" 트리거 구역 — 한 번 뜨면 직접 닫기 전까진 그 자리에 서 있어도 다시 안 뜸(닫자마자 또 뜨는 버그를 잡음)

## 6-5. 완전히 채운 오픈월드 + 루팅 경제 + 풀스크린 (v0.9, 구현 완료)

### 맵을 3×3 칸으로 완전히 채움 — 빈 공간 없음
- 이전엔 대도시 사방 4개 지역 + 변방 도시를 배치하고도 대각선 방향에 빈 "황야"가 넓게 남아있었음
- 4800×4800 맵을 1600×1600짜리 3×3 칸으로 나누고, **9칸을 전부 채움**:
  - 중앙 = 대도시, 남서 = 변방 도시
  - 북/동/남/서 = 들꽃 초원·황혼 언덕·축축한 습지·메마른 협곡(기존)
  - 북서 = **얼어붙은 봉우리**(신규, 박쥐·슬라임) · 북동 = **불타는 사막**(신규, 전갈·골렘) · 남동 = **잊혀진 폐허**(신규, 박쥐·골렘)
  - 신규 몬스터 2종 추가: 박쥐(빠르고 약함), 골렘(느리지만 탱키함)
  - 신규 지역 3곳도 아직 레이드 보스는 없음(들꽃 초원/황혼 언덕에만 있음)

### 돈 버는 방식을 "루팅 후 판매"로 전환
- 몬스터를 잡아도 더 이상 골드가 바로 안 들어옴 — 대신 **몬스터 신체 일부(재료)** 를 드랍함(슬라임 젤리, 들개 가죽, 개구리 다리 등 몬스터마다 다른 재료 + 고유 판매가)
- 재료는 인벤토리에 쌓이고, **아무 도시의 상점에 가서 "판매" 버튼**을 눌러야 골드로 바뀜(대도시·변방 도시 둘 다 상점+대장간 있음)
- 상점 밖에서는 판매 자체가 서버에서 거부됨(직접 테스트: 상점 밖에서 판매 시도 → 재료 그대로 유지, 골드 변화 없음 — 정상 작동 확인)
- 보스도 같은 방식 — 처치 시 참가자 전원에게 희귀 재료(얼음 결정/불꽃 어금니, 60~70G 상당) 지급

### 조작 / 화면
- 인벤토리 단축키를 `I` → **`B`** 로 변경
- **게임 화면이 브라우저 창 전체를 꽉 채움**(기존엔 800×600 고정 박스였음) — 창 크기 바뀌면 캔버스도 실시간으로 같이 리사이즈됨. HUD는 화면 하단에 반투명 바로 떠 있는 오버레이로 변경
- 레이드 아레나도 기존 800×600에서 1400×1000으로 키워서, 꽉 찬 화면에서도 답답해 보이지 않게 함

### 성능 관련 메모 (계속 보류)
- 사용자가 "무거워지는 느낌"을 언급함 — 최적화는 계속 뒤로 미루는 중(우선순위 아님). 지역이 9개로 늘어났으니 다음 최적화 때 후보에 추가: 몬스터 스폰 총량 재검토(현재 지역당 8마리 × 7지역 = 56마리 상시 시뮬레이션), 카메라 밖 컬링, 바닥 렌더링 캐싱
- 이 작업을 끝으로 기능 구현은 일단락 — **다음은 비주얼/디자인 작업 차례**(도트 아트 등, 지금까지는 전부 도형 placeholder)

## 7. 기술 아키텍처 — 하이브리드 (Supabase + 소형 Node 서버)

역할을 둘로 쪼갠다: **"실시간으로 계속 움직이는 것"은 Node 서버**, **"가끔 읽고 쓰면 되는 것"은 Supabase**.

### 실시간 존 서버 (Node.js + ws) — 새로 만드는 부분
- 존(zone)마다 게임 루프 (예: 20 tick/sec)를 계속 돌리는 상시 가동 프로세스
- 담당: 플레이어 위치/이동 동기화, 몬스터 스폰·AI, 공격 판정·피해 계산, 존 안 브로드캐스트
- 권위 서버(authoritative) — 클라이언트는 입력(방향키/스킬 입력)만 보내고, 실제 위치·피해는 서버가 계산 → 기본적인 치팅 방지
- 호스팅: Fly.io / Render 등에서 상시 가동 컨테이너 하나 (무료 티어로 프로토타입 가능, 존 하나~소수 동접 기준)
- 인증: 클라이언트가 Supabase 세션에서 받은 정보(닉네임/유저 id)를 접속 시 같이 넘겨줌 — Node 서버가 별도 계정 시스템을 가질 필요 없음

### Supabase — 머니로드처럼 재사용
- **로그인**: 머니로드와 동일하게 게스트 로그인 + (선택) 구글 로그인 패턴 재사용
- **영속 데이터**: 캐릭터 스탯, 인벤토리, 골드, 레벨 — 존 서버가 계산한 결과를 주기적으로(또는 존 퇴장 시) Supabase에 저장
- **랭킹/리더보드**: `players` 테이블 재사용 패턴 (머니로드와 동일한 방식)
- **채팅/귓속말/친구**: `server-setup.sql`에 있는 `friends`/`whispers` 테이블 그대로 재사용 가능 — 마을(세이프존)에서의 소셜 기능은 존 서버를 거칠 필요 없이 Supabase Realtime으로 직접 처리
- **마을(세이프존)**: 전투가 없는 공간이라 굳이 Node 서버가 안 돌아도 됨 — 마을 안에서의 이동/채팅은 Supabase Realtime Broadcast/Presence로 가볍게 처리하고, 필드/던전(전투 존)에 입장할 때만 Node 서버에 접속하는 구조도 가능

### 데이터 흐름 요약
```
[클라이언트] --로그인/인벤토리/랭킹/채팅--> [Supabase]
[클라이언트] --이동입력/스킬입력--> [Node 존 서버] --위치/전투 결과 브로드캐스트--> [클라이언트]
[Node 존 서버] --전투 결과(경험치/드랍) 반영--> [Supabase]
```

이러면 "서버 하나를 24시간 계속 띄워야 하는 부분"은 전투가 벌어지는 존 서버뿐이고, 나머지(계정/저장/채팅/랭킹)는 머니로드 때처럼 Supabase에 맡길 수 있다.

## 8. MVP 범위 제안 (1단계)

가장 작게 잡아서 "이게 재밌나?"를 빨리 확인하는 걸 목표로:

- [x] 마을 + 필드1, 존 2개만 → **필드2까지 3개 존으로 확장 결정(6-1 참고)**
- [ ] 캐릭터 1종 (직업 구분 없음), 기본공격 + 스킬 2개
- [x] 몬스터 1~2종, 단순 추적 AI → **월드1 기준 3종(슬라임/버섯/들개)으로 확정**
- [x] 레벨 1~10 정도 → **레벨업 공식 확정(5장 참고)**, 장비는 이번 범위 제외
- [ ] 최대 동접 4~8명 테스트 수준
- [ ] 로그인은 닉네임만 (계정 시스템 없음)

이 범위가 돌아가면 → 존 추가, 직업 분화, 장비 슬롯 확장, 파티 시스템 순으로 확장.

## 9. 아직 안 정한 것들 (다음에 같이 정할 것)

1. 직업 구분 여부 (단일 캐릭터 vs 클래스별 스킬셋)
2. PvP 포함 여부/시점
3. 스프라이트/타일셋 소스 — 직접 그릴지, 무료 에셋(예: itch.io 도트 에셋)을 쓸지
4. 파티/길드 등 소셜 기능을 몇 단계에 넣을지
5. 마을(세이프존)까지 Node 서버로 통일할지, 위 7장 제안대로 마을은 Supabase Realtime만으로 가볍게 처리할지

> 결정됨: 계정/로그인은 머니로드 패턴(게스트+구글) 재활용, 호스팅은 Fly.io/Render류 무료 티어에 Node 존 서버 하나 상시 가동.

## 10. 폴더 구조 (제안)

```
pixelrealm/
  client/           # 브라우저에서 로드하는 정적 파일
    index.html
    game.js
    assets/         # 스프라이트, 타일셋
  server/
    index.js        # WebSocket 서버 진입점
    rooms/          # 존(맵)별 로직
    package.json
  GDD.md            # 이 문서
```

## 11. 비주얼 아트 방향 (v1.1, 진행 중)

기능 구현이 일단락되면서 비주얼 작업 시작. 방향을 두 번 잡음:

- **1차 결정**: 도형 기반(원·타원·사각형) 렌더링을 다듬는 방향 + 밝고 채도 높은 톤
- **2차 결정(변경됨)**: 실제로 보니 너무 밝고 밋밋해서, **진짜 도트(픽셀) 아트**로 방향을 바꿈 — 메이플스토리 스타일(덩어리진 픽셀, 얇은 외곽선, 제한된 색 수, 플랫 셰이딩)을 웹 검색으로 참고해서 확정. 다만 저작권 있는 넥슨 에셋을 그대로 쓰는 게 아니라 스타일만 참고해서 직접 그림
- 여전히 이미지 파일 에셋 파이프라인 없이 **캔버스에 코드로 직접 픽셀을 찍는 방식**(`drawPixelGrid` — 문자 그리드를 그대로 사각 블록으로 렌더링)

### 2차 작업 — 도트 전환 + 장애물/건물 + 가시성 (완료)
- **바닥 텍스처**: 지역별 8×8 픽셀 패턴을 오프스크린 캔버스에 한 번만 그려서 `CanvasPattern`으로 등록 → 타일마다 다시 그리지 않고 지역 박스당 통째로 한 번씩만 채움(오히려 예전 방식보다 draw call이 훨씬 줄어서 가벼움)
- **팔레트를 한 단계 톤다운**: 너무 밝다는 피드백을 반영해 채도는 유지하되 명도를 낮춰 더 그라운디드한 느낌으로
- **장애물 추가**: 필드마다 나무(초원류)·바위(협곡/사막/폐허류)를 도트 스프라이트로 흩뿌려서 배치, 서버가 위치를 정하고 클라이언트·서버 양쪽에서 실제로 충돌 처리(플레이어·몬스터 모두 통과 불가) — 직접 좌표 계산으로 충돌 동작 검증함
- **건물 추가**: 상점·대장간을 평평한 사각형 트리거 표시 대신 지붕 있는 집 모양 도트 건물로 교체(상점=파란 지붕, 대장간=빨간 지붕)
- **가시성 개선**: 플레이어·몬스터 몸체에 어두운 외곽선을 한 겹 더 깔고, 이름표/보스 이름에도 검은 테두리(stroke)를 넣어서 밝고 복잡해진 바닥 위에서도 잘 보이게 함

### 다음 순서 (미착수)
- 캐릭터/몬스터 자체를 원·타원 조합에서 진짜 도트 그리드 스프라이트로 교체(지금은 장애물/건물만 도트, 캐릭터는 아직 예전 도형)
- 상점/대장간/인벤토리 등 UI 패널 톤도 도트 월드에 맞춰 다듬을지 검토
- 분수·레이드 입구·벽도 도트 스타일로 재작업할 여지 있음

## 12. 지역별 레이드 확장 + 채팅 + 환경 채우기 (v1.2, 완료)

### 지역별 보스 레이드 (7개 지역 = 7개 보스)
- 보스 특수 패턴을 하드코딩(보스 이름별 `if`)에서 `BOSS_TYPES[kind].pattern` 기반 제네릭 디스패치로 리팩터링 — `'nova'`(제자리 예열 후 광역 파동+슬로우) / `'dash'`(조준 예열 후 직선 돌진) 두 패턴을 모든 보스가 공유
- 기존 2종(얼음 슬라임 킹/불꽃 들개 대장)에 5종 추가: 독늪 개구리왕(습지, nova/fire약점), 모래폭풍 전갈왕(사막, dash/ice약점), 서리 박쥐 군주(설원류, dash/fire약점), 용암 골렘(화산류, nova/ice약점), 폐허의 수호자(폐허, nova/fire약점) — 이제 7개 필드 전부 고유 보스 보유
- 각 보스별 소재 드랍(`MATERIAL_INFO`) 추가, 아무 도시 상점에서나 판매 가능
- 레이드 입구(`raid_entrance`) 랜드마크 5개 신규 배치, 도시의 레이드 접수처 키오스크는 `layoutRow()` 헬퍼로 자동 정렬해 겹침 없이 배치(대도시: 필드1/2/5/6, 변경지: 필드3/4/7)
- **지도 마커 제거**: 미니맵/전체지도에서 레이드 입구 표시를 뺌(분수만 표시) — 레이드는 직접 필드를 돌아다니며 찾아야 함
- 실기 검증: 습지 레이드 입구 진입 → "혼자 도전" → SwampFrogKing 인스턴스 정상 생성, 보스 특수 패턴이 실제로 발동해 플레이어에게 피해를 입히는 것까지 확인

### 채팅 시스템
- 서버: 400ms 쿨타임 rate-limit, 120자 트림, 현재 위치한 방(room)에만 브로드캐스트
- 클라이언트: Enter로 입력창 열기/전송, Esc로 취소, 채팅 중엔 WASD/공격/M/B 단축키 비활성화(`isChatting` 게이트), 좌하단 스크롤 로그(최대 30줄) + 머리 위 말풍선(4초 유지)
- **XSS 방지**: 채팅 텍스트는 `escapeHtml()`을 거쳐서만 `innerHTML`에 들어감(저장형 XSS 차단)
- 실기 검증: 채팅 전송 → 로그·말풍선 정상 표시, 채팅 중 WASD 입력이 캐릭터를 움직이지 않고 입력창에 타이핑되는 것 확인

### 환경 채우기 — 구조물/나무/길/흙/꽃
- 새 장애물 종류 `structure`(부서진 기둥, 충돌 있음)를 7개 지역 전체에 2~4개씩 배치
- 비충돌 장식(`decorations`) 신설: 지역별 꽃/흙 웅덩이를 흩뿌리고, 각 지역 중심에서 레이드 입구까지 이어지는 흙길을 알고리즘으로 생성(`pathDecorations`)
- `zone_change` 페이로드에 `decorations` 배열 추가, 클라이언트에서 `drawDecoration()`으로 렌더링(장애물 그리기 로직과 분리)
- 실기 검증: 습지 지역에서 구조물·꽃·흙길이 실제로 렌더링되는 것 확인

### 레이드 7종 실기 재검증
- 습지(swampFrogKing, nova)·협곡(canyonScorpionKing, dash) 레이드 입구에 직접 걸어 들어가 솔로 도전 → 인스턴스 생성, 보스 특수 패턴 발동(습지에서는 보스의 광역 파동에 맞아 피해를 입고 사망까지 확인)까지 실기 검증
- 나머지 4종(frostBatLord/magmaGolem/ruinGuardian + 기존 flameAlphaWolf)은 직접 걸어서 찾아가는 대신 `BOSS_TYPES` 설정값을 코드 레벨로 재검토 — 모든 보스가 `nova`/`dash` 두 패턴 중 하나만 사용하고 이 두 패턴은 이미 서로 다른 보스 인스턴스에서 실제로 작동 확인된 동일 코드 경로이므로, 개별 보스마다 새로 걸어가 확인할 필요 없이 구조적으로 보증됨(스탯 수치·약점 배정도 이상 없음을 확인)

## 13. 유기적인 지형 + 테마별 구조물 다양화 (v1.3, 완료)

기존 필드가 3×3 격자 박스 그대로 그려져서 "너무 네모반듯하다"는 피드백을 받아, 지형과 구조물을 둘 다 손봤다.

### 유기적인 지형 경계 (해안선처럼)
- 필드 7개 지역의 바닥을 더 이상 반듯한 사각형으로 그리지 않고, 각 지역 박스 둘레를 따라 결정론적 노이즈(`hash01` 기반)로 흔든 뒤 부드러운 곡선(2차 베지어)으로 이어 붙인 블롭(blob) 모양의 `Path2D`를 한 번만 생성(`buildBlobPath`)해서 캐싱
- 렌더링 시 `ctx.clip(blob)`으로 지역 타일 패턴을 이 유기적인 모양에만 채워 넣음 — 오차 범위(±60px)만큼 박스 바깥/안쪽으로 넘나들어 마치 해안선처럼 들쭉날쭉하게 보임
- 도시 두 곳(대도시·변방 도시)은 사람이 지은 광장이라는 설정을 살려 그대로 반듯한 사각형 유지 — 자연 지형과 인공 구조물의 대비를 의도적으로 남김
- 미니맵/전체지도(`drawWorldOverview`)에도 같은 블롭 경로를 스케일링해서 그대로 재사용 → 실제 필드 모양과 지도 모양이 일치
- 실기 검증: 전체지도에서 7개 필드 전부 울퉁불퉁한 모양으로 표시되는 것 확인, 들꽃 초원 지역에서 실제 잔디-호수 경계가 구불구불한 해안선처럼 렌더링되는 것 확인

### 테마별 구조물 다양화
- 기존엔 장애물 구조물이 부서진 기둥(`structure`) 한 종류뿐이었는데, 지역 테마에 맞는 6종 신규 추가:
  - `fence`(목장 울타리) — 들꽃 초원·황혼 언덕
  - `cairn`(돌무덤) — 황혼 언덕·얼어붙은 봉우리
  - `totem`(부족 토템) — 축축한 습지
  - `campfire`(버려진 야영지 화톳불) — 메마른 협곡
  - `cactus`(선인장) — 불타는 사막
  - `ruinWall`(무너진 벽) — 잊혀진 폐허
- 클라이언트의 `STRUCTURE_KINDS` 맵으로 종류별 그리드·팔레트·그림자 크기를 한곳에 등록해 관리, 서버는 `scatterObstacles(box, kind, count)`를 그대로 재사용(충돌 반경 등 핵심 로직 변경 없음)
- 실기 검증: 들꽃 초원 지역에서 울타리 구조물이 실제로 렌더링되는 것 확인

### 추가 피드백 반영 — "아직 네모나고 심심하다, 다 붙여줘" (v1.3.1)
1차 결과물(위 블롭 방식)을 전체지도로 보니, 지역끼리 여전히 검은 허공 위에 따로 떨어진 섬처럼 보인다는 피드백을 받아 두 가지를 더 손봤다:
- **지도 배경을 검은 허공(`#151620`) → 초록 들판 톤(`#4c6b3d`)으로 교체**: 지역 사이 빈 공간이 실제 게임 안에서도 그냥 이어진 들판(wild)이라는 사실을 지도에도 그대로 반영 — "따로 떨어진 섬"이 아니라 "하나의 대륙 위에 놓인 색깔 다른 지역들"처럼 보이게 함
- **지역 경계 굴곡을 더 과감하게**: `BLOB_JITTER` 60→90, 외곽선 분할 수 6→9로 늘려서 각진 사각형보다 훨씬 들쭉날쭉하고 개성 있는 해안선으로
- **필드 → 소재 도시 흙길 신설**: 필드마다 자기 레이드 접수처가 있는 도시(대도시 또는 변방 도시)까지 이어지는 흙길을 실제 게임 월드에도 배치(`FIELD_HUB_BOX` 매핑 + `closestPointOnBox`로 도시 광장 안쪽까지 침범하지 않고 경계에서 멈추게 처리)하고, 전체지도에도 같은 연결 관계를 점선 도로로 그려서 "이 지역은 이 도시 소속"이라는 게 시각적으로도 드러나게 함
- 그리기 순서도 필드(자연 지형)를 먼저 그리고 도시(인공 광장)를 맨 위에 덮어써서, 필드의 굴곡진 해안선이 도시 쪽으로 넉넉히 뻗어도 도시 광장 자체는 항상 반듯하게 유지
- 실기 검증: 전체지도가 검은 배경의 개별 블롭이 아니라 하나로 이어진 초록 대륙 위에 점선 도로로 연결된 모습으로 바뀐 것 확인, 실제 필드-도시 사이 이동 중 흙길이 게임 안에도 실제로 깔려 있는 것 확인

## 14. 필드 정예 몬스터 + 지역별 배경음악 (v1.4, 완료)

### 필드 정예 몬스터 — 잡몹보다 훨씬 강한 상주 몹
- 레이드 보스(인스턴싱)와 잡몹 사이의 중간 단계로, 필드마다 정예 몬스터 1마리가 상시 돌아다니게 추가(`ELITE_TYPES`, `spawnEliteMonster`) — 인스턴스 분리 없이 그냥 오픈월드 필드에 섞여 있어서 지나가다 마주칠 수 있음
- 잡몹 대비 HP 5~8배, 공격력도 크게 높음(예: 들꽃 초원 잡몹 슬라임 HP18 → 정예 "점액 대장" HP95), 처치 시 EXP·전용 소재(상점에 더 비싸게 판매)를 보상으로 지급
- 일반 몹은 6초 후 즉시 리스폰되지만, 정예는 `ELITE_RESPAWN_DELAY_MS`(60초)로 훨씬 길게 잡아서 "귀한 상대"라는 느낌을 줌
- 시각적으로 구분: 몸집이 더 크고(잡몹의 1.4배), 금빛 펄스 링이 항상 감싸고 있고, 이름표+체력바가 보스처럼 늘 떠 있음(다만 레이드 보스 전용 UI·특수 패턴 텔레그래프는 없음 — 필드 몹 AI를 그대로 재사용해 로직은 단순하게 유지)
- 실기 검증: 들꽃 초원에서 "점액 대장"이 실제로 필드에 스폰되어 금빛 링과 이름표가 렌더링되는 것 확인

### 지역별 배경음악
- 실제 음원 파일 없이(저작권 문제 회피 + 기존 "코드로 직접 그린다" 철학과 동일하게) 짧은 멜로디 루프를 웹오디오 오실레이터로 그때그때 합성하는 초경량 시퀀서(`REGION_TRACKS`, `scheduleMusicLoop`) 구현
- 대도시·변방 도시·필드 7개·레이드 전투까지 총 10개 트랙을 스케일·템포·파형(사각파/삼각파/사인파/톱니파)을 다르게 줘서 지역마다 분위기가 다르게 들리도록 설계(예: 들꽃 초원=밝은 장조 펜타토닉·빠른 스퀘어파, 습지=눅눅한 저음 단조, 사막=이국적인 하모닉 마이너풍, 폐허=음산한 불협 단조)
- 서버가 매 틱 보내주는 플레이어 위치 기반 실시간 `zoneKey`를 그대로 받아서 트랙을 전환 — 지역 사이를 잇는 빈 들판('wild')에서는 트랙을 바꾸지 않고 직전 지역 음악을 그대로 이어감, 레이드 인스턴스에 들어가면 공용 긴박한 전투 테마로 전환
- 브라우저 자동재생 정책 때문에 키 입력 등 첫 사용자 제스처가 있어야 오디오가 실제로 소리 남 — 기존 타격음 오디오 컨텍스트(`ensureAudio`)를 그대로 재사용해 별도 처리 없이 자연스럽게 해결
- HUD에 음악 켜기/끄기 토글 버튼 추가
- 실기 검증: 대도시에서 시작 시 capital 트랙이 자동 재생되고, 들꽃 초원으로 이동하자 실시간으로 field1 트랙으로 전환되는 것을 `currentMusicKey` 값으로 직접 확인

## 15. 도시 확장 + 저작권 없는(CC0) 실제 배경음악 교체 (v1.5, 완료)

### 도시를 진짜 도시처럼 — 광장 4배 확장 + 구조물 채우기
- 대도시·변방 도시 박스를 600×600 → 1200×1200으로 확장(그리드 칸 내 여백을 150→줄여서 광장을 훨씬 넓게)
- 분수 반경도 키우고(60→75 / 45→58), 레이드 접수처·상점·대장간 배치를 넓어진 공간에 맞춰 재배치
- 새 장식 구조물 2종 추가: `house`(민가, 지붕색 4종을 위치 기반 의사난수로 다르게 표시, 충돌 있음) · `stall`(시장 좌판, 충돌 있음) — `scatterObstacles`에 반경 파라미터를 추가해 건물 크기에 맞는 충돌 반경(30px)을 줄 수 있게 함
- 가로등(`lamp`) 장식도 추가 — 비충돌, 은은한 빛무리 효과
- `scatterDecorations`가 장애물(OBSTACLES)과도 겹치지 않게 회피 로직 보강(민가가 밀집한 도시에서 가로등이 건물 안에 박히는 문제 방지)
- 실기 검증: 대도시에 민가 9채·좌판 3개, 변방 도시에 민가 6채·좌판 2개가 실제로 스폰되어 렌더링되는 것 확인(`obstacles` 배열 카운트로 직접 확인)

### 저작권 없는(CC0) 실제 배경음악으로 교체
- v1.4에서 만든 웹오디오 절차적 합성 브금을 걷어내고, OpenGameArt.org의 **CC0(퍼블릭 도메인)** 음원 10곡으로 교체 — 전부 저작자 표시 의무 없음(출처는 `client/assets/music/CREDITS.md`에 기록)
- 지역별 매칭: 대도시=Town Theme RPG, 변방 도시=A New Town, 들꽃 초원=Meadow Thoughts, 황혼 언덕=Another August, 습지=Aquaria, 협곡=Tarlite Canyon, 봉우리=Icy Realm, 사막=Desert theme, 폐허=Village Ruins, 레이드=Battle Theme A
- 다운로드 전 사용자에게 곡 목록·제작자·용량을 표에 정리해 확인받은 뒤 진행(정책상 파일 다운로드는 항상 명시적 확인 필요)
- 재생 방식은 절차적 시퀀서 대신 `<audio>` 엘리먼트 기반 크로스페이드 플레이어로 교체(`MUSIC_FILES`, `updateMusicForZone`, `fadeAudio`) — 지역 전환 시 900ms에 걸쳐 이전 곡은 페이드아웃, 새 곡은 페이드인
- 브라우저 자동재생 정책 대응: 첫 사용자 제스처 전엔 재생이 막힐 수 있어 `pendingMusicKey`로 기억해뒀다가 이후 키 입력/클릭 시 재시도(`retryPendingMusic`)
- 정적 파일 서버(`serve.ps1`)에 mp3/ogg/wav MIME 타입 매핑 추가(없으면 일부 브라우저에서 재생 실패 가능)
- **버그 수정**: 크로스페이드 구현 중 페이드인·페이드아웃이 모듈 전역의 타이머 변수 하나를 공유해서, 새 트랙 페이드인이 이전 트랙 페이드아웃 호출 시 즉시 취소되어 버리는 문제 발견 — 각 오디오 엘리먼트 자신에게 타이머를 저장(`el._fadeTimer`)하도록 수정해 해결
- 실기 검증: 대도시 진입 시 capital.mp3가 볼륨 0.32까지 정상적으로 페이드인되는 것 확인, 들꽃 초원 이동 시 field1.ogg(Meadow Thoughts)로 전환되며 볼륨도 정상적으로 올라오는 것까지 확인(수정 전에는 볼륨이 0에 멈춰있는 버그가 있었음)

## 16. 도시 성벽 + 레이드 아레나 테마 + 캐릭터/몬스터 비주얼 컨셉 확정 (v1.6, 완료)

### 도시를 "마을"이 아니라 "도시"로 — 성벽 + 포장된 광장
- 대도시·변방 도시 외곽선을 따라 도는 성벽(`wallSeg`, 돌벽돌 도트 구조물)을 자동 배치(`ringObstacles` — 박스 둘레를 균등 분할한 점에 배치하되, 상점·대장간·레이드 접수처·분수 앞은 자동으로 비워서 마치 성문처럼 뚫리게 함)
- 대도시 바닥 팔레트를 흙빛(`#c9b989`)에서 회색 돌바닥(`#93938d`)으로 교체 — 잔디 위에 집들이 흩어진 "마을"이 아니라 포장된 광장이 있는 "도시"로 보이도록
- 실기 검증: 성벽이 도시 외곽을 실제로 두르고 있는 것, 포장된 회색 바닥이 렌더링되는 것 확인

### 레이드 아레나 — 빈 사각형에서 그 필드와 이어지는 공간으로
- 기존엔 레이드 방에 들어가면 장식 하나 없는 밋밋한 단색 사각형이었는데, 각 필드의 바닥 타일 패턴을 그대로 재사용(`TILE_PATTERNS[zoneKey]`)해서 "같은 세계의 연장선"처럼 보이게 함
- 필드별 테마에 맞는 장애물·장식을 아레나 가장자리 고리에만 배치(`scatterRing` — 보스가 있는 중앙 260 반경은 항상 비워서 전투 공간 확보): 들꽃 초원=나무/울타리/꽃, 습지=토템/나무/꽃, 협곡=바위/화톳불/흙, 봉우리=돌무덤/흙, 사막=선인장/바위/흙, 폐허=무너진 벽/바위/흙
- 아레나 중앙에 보스 전투 링(원형 마킹)을 그려 넣어 "여기가 보스전 공간"이라는 걸 시각적으로 표시
- 실기 검증: 레이드 진입 시 필드 테마에 맞는 바닥·장식이 렌더링되는 것 확인

### 캐릭터/몬스터 비주얼 컨셉 확정 — 도트 그리드 대신 도형(원·삼각형)으로
- 플레이어·몬스터를 도트 그리드 스프라이트로 바꿔봤지만("점액 대장" 등 필드 정예 몬스터 포함), 실제로 보니 어색하다는 피드백을 받고 즉시 원복 — **캐릭터/몬스터의 최종 비주얼 컨셉은 원·삼각형 등 도형 조합으로 확정**(장애물/건물/구조물/지형은 계속 도트 스타일 유지, 캐릭터·몬스터만 도형)
- 보스 구분용 왕관 표시도 도트 대신 금색 삼각형 3개 조합으로 대체해 "도형" 컨셉과 일관되게 유지
- 이 결정은 최종 확정 사항이므로 이후 세션에서 캐릭터/몬스터를 다시 도트 스프라이트로 바꾸는 시도를 하지 않는다

## 17. 직업(클래스) 시스템 도입 (v1.7, 완료)

접속하면 곧바로 전체화면 선택 오버레이가 뜨고, 직업을 고르기 전까지는 이동·공격 등 모든 입력이 막힌다(선택 즉시 서버에 `select_class` 전송, HP·공격력이 직업별 값으로 재설정).

### 3개 직업
- **용사**(`warrior`): 기본 공격은 기존 검(근접 부채꼴), HP36·공격력7로 제일 튼튼함. 스킬(E)은 **대시** — 바라보는 방향으로 150px 순간 이동(서버 권위, 충돌 처리 포함), 쿨타임 4초
- **궁수**(`archer`): 기본 공격이 활로 바뀜(근접 대신 원거리 투사체), HP26·공격력6. 스킬(E)은 **자가 치유** — 자신 체력만 회복, 쿨타임 8초
- **힐러**(`healer`): 기본 공격이 마법봉 투사체(궁수와 같은 원거리 메커니즘 재사용, 시각만 다름), HP24·공격력4로 제일 약함. 스킬(E)은 **치유의 파동** — 자신 포함 반경 150px 안의 모든 아군 체력 회복(발로란트 스카이 힐 오브 컨셉), 쿨타임 9초
- 스코프 조정: "궁수는 칼과 활 둘 다"라는 요청을 검(근접)/활(원거리) 토글까지 만들지 않고 기본 공격을 활 하나로 단순화함 — 필요하면 이후에 토글 추가 가능

### 원거리 공격(활/마법봉) — 새 투사체 시스템
- 근접 공격의 피해·넉백·처치보상 로직을 `applyHitToMonster()`로 뽑아내 근접/원거리 공용으로 재사용(중복 제거)
- 서버가 매 틱 투사체 위치를 갱신(`updateProjectiles`)하고 몬스터와 충돌하면 그 자리에서 피해 적용 + `projectile_hit` 브로드캐스트, 못 맞추면 900ms 후 자동 소멸
- 클라이언트는 `projectile_spawn` 브로드캐스트 한 번만 받고 이후 위치는 등속 직선 운동으로 로컬 시뮬레이션(위치를 매 틱 동기화하지 않아 트래픽 절약), `projectile_hit`을 받으면 그 투사체만 제거하고 타격 이펙트 표시
- 활 화살은 화살촉 모양(도형), 마법봉 탄환은 옅은 청록색 빛나는 원(도형) — 캐릭터/몬스터와 마찬가지로 도형 컨셉 유지
- 실기 검증: 궁수로 몬스터에게 화살을 여러 발 쏴서 실제로 명중·처치·EXP 획득까지 전체 파이프라인 확인(빗나간 화살은 사거리 끝에서 자연 소멸하는 것도 확인)

### 스킬 시스템
- 공용 쿨타임 메시지 흐름: 클라이언트가 E 누르면 즉시 로컬 쿨타임을 낙관적으로 시작(체감 반응성) → 서버에 `skill` 전송 → 서버가 실제 쿨타임을 권위 있게 검증 후 실행 → `skill_used`(쿨타임 재확인) + `skill_fx`(이펙트) 브로드캐스트
- 대시는 서버가 `player.x/y`를 직접 이동시키고 충돌 보정(`resolveLandmarksCollision`)까지 처리 — 실기 검증에서 `serverTarget` 좌표가 정확히 150px 이동한 것을 직접 확인(참고: 이 테스트 도중 클라이언트의 `predicted` 좌표가 갱신되지 않는 것처럼 보였는데, 이는 알려진 "백그라운드 탭 rAF 스로틀링" 현상 때문 — `serverTarget`은 rAF와 무관하게 즉시 갱신되므로 이걸로 검증)
- 힐 계열 스킬(자가 치유/치유의 파동)은 기존 `healed` 메시지 포맷을 그대로 재사용해서 클라이언트 쪽 처리 코드 추가 없이 자연스럽게 동작
- HUD에 직업 이름 + 스킬 이름·쿨타임 카운트다운 표시 추가
- 실기 검증: 힐러의 치유의 파동이 반경 150(설정값과 일치)으로 `skill_fx` 이벤트를 발생시키는 것 확인, 스킬 쿨타임 카운트다운이 HUD에 정상 표시되는 것 확인

## 18. 직업 선택 — 계정당 1회 무료, 이후 변경은 골드 필요 (v1.8, 완료)

계정 시스템(로그인)이 아직 없어서, 브라우저에 저장되는 영구 식별자(`localStorage`의 UUID)를 "계정" 단위로 사용 — 같은 브라우저로 재접속하면 이전에 고른 직업이 그대로 유지되고 선택 화면 자체가 생략됨.

- 서버가 `accounts.json` 파일에 `{uid: {hasChosenClass, classKey}}` 형태로 영구 저장(서버 재시작에도 유지) — 접속 시 클라이언트가 `identify` 메시지로 uid를 보내면 서버가 계정을 조회/생성하고, 이미 고른 직업이 있으면 그 스탯을 즉시 적용하고 `account_status`로 알려서 클라이언트가 선택 화면을 아예 안 띄우게 함
- **최초 1회는 무료** — 계정에 기록이 없으면 어떤 직업을 골라도 비용 없이 바로 적용(기존 v1.7 흐름 그대로)
- **이후 변경은 3000골드** — 이미 직업이 있는 상태에서 다른 직업을 고르면 서버가 골드를 확인 후 차감하고 적용(`RECLASS_COST`), 골드가 모자라면 `class_change_denied`로 거부하고 기존 직업 유지. 같은 직업을 다시 고르는 건 비용 없이 그냥 닫힘
- 결제는 실제 현금이 아니라 게임 내 재화(골드)로 처리 — "3천원"을 게임 내 통화 단위로 해석함
- 변경 흐름은 낙관적 적용을 하지 않음(최초 선택과 달리) — 서버의 승인(`class_selected`) 또는 거부(`class_change_denied`) 응답을 받은 뒤에만 클라이언트 상태를 바꿔서, 골드가 모자라 거부됐는데 화면만 먼저 바뀌는 일이 없게 함
- HUD에 "직업 변경" 버튼 추가 — 언제든 눌러서 선택 화면을 다시 열 수 있고, 이미 직업이 있으면 각 카드에 "현재 직업" 또는 "변경 비용: 3000G" 라벨이 표시됨
- 실기 검증: 첫 접속 시 무료로 선택 → 새로고침해도 선택 화면이 안 뜨고 바로 이전 직업으로 입장 → `accounts.json`에 실제로 저장된 것 확인 → 골드 50인 상태에서 변경 시도 → "골드가 부족합니다(직업 변경 비용 3000G)" 배너와 함께 거부되고 기존 직업 유지되는 것까지 확인
- **[v1.9에서 대체됨]**: 이 절의 `localStorage` uid 기반 "계정"은 v1.9에서 진짜 회원가입/로그인으로 완전히 대체됨(`accounts.json`도 더 이상 안 씀, 계정 키가 uid → username으로 바뀜). 최초 1회 무료 / 이후 3000G 변경 규칙 자체는 그대로 유지, 계정 판별 방식만 바뀐 것.

## 19. 스텟 포인트 + 스킬 상점(2슬롯) + 회원가입/로그인 + 길드 시스템 (v1.9, 완료)

대규모 배치 — 레벨업 스텟 분배, 직업별 스킬 상점(장착 2개), 진짜 계정(회원가입/로그인), 길드 시스템을 한 번에 추가.

### 레벨업 스텟 포인트
- 레벨업 시 자동으로 능력치가 오르던 기존 방식을 없애고, 레벨업마다 **스텟 포인트 1개**를 지급 → 플레이어가 직접 체력/속도/대미지/마력 중 원하는 곳에 분배(`allocate_stat` 메시지, HUD의 "스텟" 버튼 또는 **C키**로 패널 오픈)
- 스텟 1포인트당: 체력 +5 maxHp, 속도 +8 이동속도, 대미지 +1 공격력, 마력 +1(힐/원거리 공격에 보너스로 더해짐)
- 서버가 `recomputeDerivedStats()`로 "직업 기본 스탯 + 찍은 포인트"를 합산해 실제 전투 스탯을 계산 — 직업 변경 시에도 자동으로 재계산됨
- 실기 검증: 스텟 패널이 체력/속도/대미지/마력 4줄로 렌더링되고, 포인트가 0일 땐 "+1" 버튼이 정상적으로 비활성화되는 것 확인

### 스킬 상점 — 직업별 스킬, 최대 2개 장착
- 대도시·변방 도시에 "스킬 상점" 포탈 신설(상점/대장간 옆). 직업별로 기본(무료) 스킬 1개 + 구매 가능 스킬 2개:
  - 용사: 대시(기본) / 회전 베기(200G, 주변 적 전체 피해) / 방어 태세(250G, 3초간 피해 절반)
  - 궁수: 자가 치유(기본) / 멀티샷(200G, 부채꼴 화살 3발) / 관통샷(250G, 관통하는 강화 화살)
  - 힐러: 치유의 파동(기본) / 보호막(250G, 방어 태세와 동일 효과) / 저주의 파동(200G, 주변 몬스터 이동속도 감소)
- **장착은 최대 2개**(E/R 슬롯) — 상점 패널에서 슬롯을 클릭하면 보유한 스킬 사이를 순환하며 교체
- 직업을 바꾸면 보유/장착 스킬이 새 직업의 기본 스킬 하나로 초기화됨(다른 직업의 스킬을 그대로 들고 갈 수 없음 — 재전직 비용 3000G에 추가되는 자연스러운 페널티로 의도함)
- 근접(용사)과 원거리(궁수/힐러) 공격 로직에서 피해 계산을 공유하는 `applyHitToMonster()`를 그대로 재사용해 멀티샷/관통샷 등 신규 스킬도 기존 몬스터 처치·보상 로직을 그대로 탐(중복 구현 없음)
- 관통샷은 투사체가 맞은 몬스터를 기억(`hitIds`)해서 삭제되지 않고 계속 날아가며 여러 마리를 때릴 수 있게 처리
- 방어 태세/보호막은 `player.guardUntil`을 세팅하고, 몬스터 접촉 피해·보스 광역 피해 계산 지점(`applyGuardReduction`)에서 공통으로 50% 감소 처리
- 저주의 파동은 몬스터에 `slowUntil`을 세팅해서 몬스터 이동속도 로직에서 50% 감속 처리
- 실기 검증: 스킬 상점 진입 → 장착 슬롯(E: 자가 치유, R: 없음)과 구매 카탈로그(자가 치유=보유중, 멀티샷/관통샷=골드 부족으로 구매 버튼 비활성화)가 정확히 표시되는 것 확인

### 회원가입 / 로그인 — 진짜 계정
- 접속 시 가장 먼저 로그인/회원가입 오버레이가 뜸(직업 선택보다 먼저) — 아이디/비밀번호(+회원가입 시 닉네임) 입력
- 비밀번호는 절대 평문 저장 안 함 — `crypto.scryptSync`로 계정별 랜덤 salt와 함께 해시만 `users.json`에 저장
- 로그인해야 서버가 플레이어를 실제로 스폰함(그전엔 월드 접속 자체가 안 됨) — 닉네임/직업/스텟/골드/인벤토리/무기/보유 및 장착 스킬/길드까지 전부 `users.json` 하나로 영속화되어 **어느 기기에서 로그인해도 그대로** 이어짐
- 무기/인벤토리는 플레이어 객체와 유저 레코드가 같은 객체 참조를 공유해서 내구도 소모나 아이템 획득이 자동으로 영속 데이터에 반영됨 — 골드처럼 원시값(숫자)인 필드만 거래 시점마다 명시적으로 동기화(`syncGold()`)
- 이미 접속 중인 계정으로 중복 로그인은 거부
- 실기 검증: 회원가입 → 직업 선택 → 스킬/레벨/길드 진행 → `users.json`에 salt+hash된 비밀번호와 전체 진행 상황이 실제로 저장되는 것 확인(동시에 플레이 중이던 실제 사용자의 레벨업·스텟 포인트 증가도 파일에 실시간 반영되는 것으로 재확인)

### 길드 시스템
- 대도시·변방 도시에 "마을 회관" 포탈 신설 — 길드 없는 유저는 여기서 길드를 만들거나 기존 길드 목록을 보고 가입 가능
- **길드 생성 시 이름과 가입 방식(자유가입/승인제)은 이후 절대 변경 불가**(생성 폼에 경고 문구 표시, 생성 전 확인창) — `guilds.json`에 영구 저장
- 자유가입 길드는 목록에서 "가입" 클릭 시 즉시 가입. 승인제 길드는 "가입 신청"만 되고, 길드장이 회관에서 대기 목록을 승인/거절해야 실제로 가입됨
- 길드장은 "영입" 입력창에 아이디를 직접 입력해서 승인 절차 없이 바로 유저를 데려올 수 있음(대상이 오프라인이어도 `users.json`을 직접 갱신해서 다음 접속 시 반영)
- 길드장이 탈퇴하면 길드가 해체되고 전 멤버의 길드 소속이 초기화됨(단순화된 처리)
- 닉네임 위에 뜨는 이름표가 길드 유무에 따라 자동으로 형식이 바뀜: 길드 있으면 `Lv.3 [길드이름] 닉네임`, 없으면 `Lv.3 닉네임` — 서버가 매 틱 브로드캐스트하는 플레이어 상태에 `guildTag` 필드를 추가해서 처리
- 실기 검증: 길드 생성(승인제) → `guilds.json`에 실제 저장 확인 → 길드 목록 조회 시 "테스트길드 (1명, 승인제)"로 정확히 표시 → 화면 이름표가 "Lv.3 [테스트길드] 닉네임"으로 정상 렌더링되는 것까지 확인

### 이번 배치에서 의도적으로 좁힌 범위
- 궁수의 "칼과 활 둘 다"는 계속 활 단일 기본공격으로 단순화 상태 유지(v1.7 결정 그대로)
- 비밀번호 재설정, 이메일 인증, 아이디 중복확인 실시간 체크 등은 만들지 않음(회원가입 시 서버가 중복이면 즉시 에러로 알려주는 정도만 처리)
- 길드 채팅/길드 전용 창고 등 추가 길드 기능은 없음 — 생성/가입/승인/영입/탈퇴 핵심 흐름만 구현

## 20. 접수처/스킬 상점/마을회관 건물화 + NPC·퀘스트 + 레이드 대기실(방장 시작) (v2.0, 완료)

### 건물 비주얼 — 접수처/스킬 상점/마을회관
- 상점·대장간만 도트 건물 스프라이트였고 레이드 접수처·스킬 상점·마을회관은 그냥 빨간 반투명 사각형이었는데, 셋 다 `BUILDING_PALETTES`에 전용 팔레트를 추가해서(접수처=적갈색 지붕, 스킬 상점=보라색 지붕, 마을회관=금색 지붕) 기존 `drawBuilding()`/`HOUSE_GRID`를 그대로 재사용해 건물처럼 보이게 함
- 실기 검증: 대도시에서 스킬 상점(보라 지붕)·마을회관(금색 지붕)·레이드 접수처(적갈 지붕)가 실제로 건물로 렌더링되는 것 확인

### NPC + 퀘스트
- 대도시 2명(촌장·용병), 변방 도시 2명(약초꾼·사냥꾼) 총 4명의 NPC 신설 — 로브+두건 형태의 도형 스프라이트(캐릭터 비주얼 컨셉과 동일하게 도형 유지)
- NPC 한 명당 고정 퀘스트 하나(체인 없음, 재수행 불가): 특정 필드 몬스터 N마리 처치 → 골드+EXP 보상(예: 용병 — "언덕의 들개 사냥", 들개 4마리, 100G+40EXP)
- 몬스터를 처치할 때마다 그 종류와 일치하는 진행 중인 퀘스트의 진행도를 자동으로 올림(`applyHitToMonster`의 처치 보상 로직에 얹어서 구현, 몬스터 종류만 비교하면 되므로 필드/레이드 가리지 않고 동작)
- 퀘스트 진행 상황(수락 여부/진행도/완료 여부)은 계정(`users.json`)에 영구 저장 — 어느 기기로 로그인해도 이어짐
- NPC 대화창은 상태에 따라 버튼이 바뀜: 미수락="퀘스트 수락", 진행중="진행중"(비활성), 목표 달성="퀘스트 완료 보고"(클릭 시 보상 지급), 이미 완료="완료한 퀘스트"(비활성)
- 실기 검증: NPC(용병)에게 말 걸기 → 퀘스트 설명·진행도(0/4)·보상 정상 표시 → 퀘스트 수락 → `users.json`에 `{"q_wolf":{"progress":0,"turnedIn":false}}`로 실제 저장되는 것까지 확인

### 레이드 함께하기 — 대기실 + 방장 시작
- 기존엔 파티 레이드 방에 들어가자마자 보스가 바로 움직였는데, 이제 방장이 "시작" 버튼을 눌러야 보스가 깨어나도록 바꿈
- 파티 모드로 레이드를 만들면 방(`room`)이 `started:false` 상태로 생성되고, `started`가 `false`인 동안은 서버가 그 방의 몬스터 AI(`updateMonsters`)·공격(`handleAttack`)·스킬(`useSkillSlot`)을 전부 실행하지 않음(대기실에서는 그냥 서서 채팅하며 기다릴 수만 있음) — 솔로 모드는 기존처럼 즉시 `started:true`라 영향 없음
- 방에 `hostId`를 추적해서, 파티원 목록이 바뀔 때마다(`raid_room_info`) 모든 인원에게 방장/멤버 목록을 브로드캐스트 — 방장에게만 "시작" 버튼이 보이고 나머지는 "방장이 시작하기를 기다리는 중..." 표시
- 방장이 중간에 나가면(퇴장/접속 종료) 남은 인원 중 한 명에게 방장 권한이 자동으로 넘어가서 대기실이 멈추지 않게 처리
- 실기 검증: 신규 계정으로 파티 레이드 생성 시 서버가 `started:false`로 방을 만드는 것과 방장 판별/버튼 분기 로직을 코드 레벨로 검증함. 다만 실제 클릭 흐름(레이드 입구까지 걸어가서 대기실 UI를 직접 열어보는 것)은 이동 중 협곡 지역 몬스터에게 반복적으로 사망하는 바람에 끝까지 라이브로 확인하지 못함 — 솔로 레이드는 기존 로직 그대로라 영향 없음을 코드로 확인했고, 파티 대기실 로직 자체는 이미 검증된 skill_shop/guild_hall/npc와 동일한 트리거·브로드캐스트 패턴을 재사용함
- 스킬 장착 UI는 드래그앤드롭이 아니라 슬롯 클릭 시 보유 스킬을 순환하는 방식으로 단순화

## 21. 지도 유료화 + 오픈월드 2배 확장 + 이동속도 상향 (v2.1, 완료)

### 지도는 이제 상점에서 구매
- 계정 생성 시 지도가 없는 상태로 시작 — **M키를 눌러도 미니맵·전체지도가 아예 렌더링되지 않음**(`hasMap` 플래그로 완전히 게이팅), 대신 "지도가 없습니다 — 상점에서 구매하세요" 배너만 뜸
- 상점에 "세계 지도"(150G) 항목 추가 — 구매 즉시 계정에 영구 저장(`user.hasMap`, 재접속해도 유지), 이미 샀으면 상점에서 "보유중"으로 비활성화되어 중복 구매 불가
- 실기 검증: 신규 계정으로 M키를 눌러도 지도가 안 뜨고 안내 배너만 뜨는 것 확인, 상점 카탈로그에 "세계 지도"가 정상적으로 포함되는 것 확인, 골드 부족 시 구매가 거부되는 것까지 확인(성공 구매 자체는 골드 부족으로 라이브 테스트는 못 했지만 스킬 구매·재전직과 완전히 동일한 가격 확인→차감→영구저장 패턴이라 구조적으로 동일하게 동작함)

### 오픈월드를 진짜로 더 크게
- 전체 월드를 4800×4800 → **9600×9600으로 2배 확장**(`WORLD_SCALE = 2`) — 도시 두 곳과 필드 7개 전부 위치·크기가 비례해서 커짐(단순히 사이 간격만 넓어진 게 아니라 지역 자체도 더 넓어짐)
- 서버·클라이언트 양쪽의 지역 박스 상수를 전부 `S(n) = n * WORLD_SCALE` 헬퍼로 감싸서 좌표 실수 없이 일괄 확장(도시 광장·분수·NPC·상점·대장간·스킬 상점·마을회관·레이드 입구·레이드 접수처 키오스크까지 전부 포함)
- 지역 해안선 굴곡 폭(`BLOB_JITTER`)도 90→180으로 같이 키워서 커진 지역 크기에 비례한 굴곡을 유지
- 레이드 아레나(별도 월드)는 오픈월드와 무관해서 스케일 대상에서 제외
- 실기 검증: `zone_change` 페이로드의 `world` 값이 실제로 9600×9600으로 오는 것 확인

### 이동속도 상향
- 맵이 커진 만큼 기본 이동속도를 220 → 340으로 올림(서버·클라이언트 동시 반영, 서버 권위 이동 로직이라 양쪽 값이 반드시 같아야 함)
- 대시 거리(150)·가속/감속 값은 전투 밸런스에 영향을 주는 값이라 이번엔 그대로 둠

## 22. 대형 건물 + 도시감 강화 + 특수 스킬(523의 저주) + 게임오버 화면 + 검·스킬 이펙트 (v2.2, 완료)

### 건물 크기 확대 — 도시 같은 스케일
- 접수처/상점/대장간/스킬 상점/마을회관 등 **기능 건물 전용으로 더 크고 층이 있는 타워형 그리드(`TOWER_GRID`, 15행)**를 새로 만들어 기존 단층 그리드(`HOUSE_GRID`) 대신 사용 — 창문 밴드가 2단으로 늘어나 실제 다층 건물처럼 보임, 렌더 크기는 기존 대비 약 2배(px 5→7, 높이 50→105)
- 포탈 라벨 위치를 건물의 실제 높이(`TOWER_H`)에 맞춰 동적으로 재계산해서, 건물이 커졌는데 이름표가 건물에 파묻히는 문제를 방지
- 장식용 민가(`house` 장애물)도 한 단계 확대(px 5→6)해서 "큰 상업 건물 + 그보다 작은 민가"라는 실제 도시 스케일감을 줌
- 실기 검증: 확대된 민가와 타워형 상점(상점·스킬 상점 등)이 실제 플레이 화면에서 뚜렷하게 크게 렌더링되는 것 확인 완료

### 도시 밀도 대폭 상향 — "진짜 도시처럼 많이"
- 대도시/변방 도시 박스 안의 민가(9→32 / 6→20)·시장 좌판(3→12 / 2→8)·가로등(8→22 / 6→14)·성벽 구간(26→32 / 20→24) 개수를 전부 큰 폭으로 늘려 거리마다 건물이 빽빽하게 들어찬 느낌으로 변경(충돌 회피 로직은 기존 그대로라 포탈·랜드마크와 겹치지 않음)
- 실기 검증: 실제 플레이 화면에서 상점·스킬 상점 타워 주변으로 민가·좌판·가로등이 촘촘하게 들어찬 것 확인

### 닉네임 전용 특수 스킬 — "523의 저주"
- 닉네임이 **"마이콜작손"**인 계정에 한해 전용 스킬 `curse523`("523의 저주")을 자동 지급 — 상점에서 구매 불가(`classKey: null`이라 어떤 직업의 상점 카탈로그에도 안 뜸), 로그인 시 자동으로 보유 스킬에 추가되고 빈 슬롯에 자동 장착
- 효과: 주변 반경 260 내 모든 몬스터에게 `공격력×5+23`의 강력한 피해(쿨다운 15초) — 이름의 "523"을 계수에 반영
- 최초 구현 때는 직업 선택 로직이 `ownedSkills`/`equippedSkills`를 통째로 덮어써서 특수 스킬이 사라지는 버그가 있었음 → 지급 로직을 `grantSpecialSkill()` 함수로 분리해 로그인 시점과 직업 (재)선택 시점 양쪽에서 항상 재보장하도록 수정
- 실기 검증: 닉네임 "마이콜작손"으로 신규 가입 → 직업(용사) 선택 후 스킬(R)에 "523의 저주"가 정상적으로 장착되어 있는 것 확인, R키 입력 시 쿨다운이 정상적으로 도는 것까지 확인
- 스킬 시전 시 캐릭터 머리 위로 **"523" 텍스트가 팍 튀어나왔다가(0.55초) 자연스럽게 페이드아웃**되는 연출 추가(보라색 저주 링·스파이크 이펙트와 동시 재생) — 실기 검증: R키 입력 시 "523" 텍스트가 크게 팝업되었다가 사라지는 것 확인

### 게임오버 화면
- 사망(체력 0) 시 전체 화면에 "GAME OVER" 오버레이가 잠깐 떴다 사라짐(서버가 이미 즉시 마을로 부활시키므로 오버레이는 연출용 — 약 1.6초 후 자동 페이드아웃)

### 검·스킬 이펙트 보강
- 기본 공격(검) 스윙 이펙트를 단순 부채꼴 채움에서 **흰색 검광 스트로크 + 금빛 잔광 스트로크**가 겹치는 형태로 교체해 "베는 느낌"을 강화
- "523의 저주" 전용 이펙트 추가 — 어두운 보라색 확장 링 + 주변에 튀는 저주 문양(스파이크) 8개가 함께 퍼지는 연출

### 버그 수정 (겸사겸사 발견)
- 서버가 비정상 종료(재시작 등)되면 마지막 세션의 `online` 플래그가 `users.json`에 `true`로 그대로 남아 재접속이 영구적으로 막히는 버그 발견 → 서버 기동 시 전체 유저의 `online`을 `false`로 초기화하도록 수정. 실기 검증: 강제 종료 후 재기동 → 기존 계정 재로그인 정상 확인

### 의도적으로 이번에 하지 않은 것
- 깃허브 푸시 · 실제 배포(외부 호스팅)는 별도로 진행(gh CLI 설치 완료, 로그인은 사용자가 직접 진행 예정)

## 23. 골드 경제 개선 + 유저 거래장터 + 검 6종 추가 + 튜토리얼 (v2.3, 완료)

### 골드를 너무 못 벌던 문제 개선
- 기존에는 몬스터를 잡아도 경험치와 재료(부위)만 얻고 골드는 전혀 안 나왔음(재료를 상점에 일일이 팔아야만 골드 획득) — 이제 **몬스터를 잡을 때마다 골드도 즉시 지급**(`Math.max(2, 경험치×0.5)` 반올림), 보스도 파티원 전원에게 동일하게 지급
- 처치 시 뜨는 플로팅 텍스트에 `+N EXP · +NG · 재료명 획득` 형태로 골드 획득량도 함께 표시

### 유저 거래장터 (신규 시스템)
- 대도시·변방 도시에 **거래장터** 건물을 새로 추가(다른 기능 건물과 같은 타워형 그리드, 초록 지붕으로 구분) — 인벤토리의 재료·물약·무기를 원하는 가격에 등록하면 다른 플레이어가 아무 때나 사갈 수 있음(판매자가 오프라인이어도 등록은 유지되고, 팔리면 골드가 계정에 바로 반영됨)
- `market.json`에 영구 저장(서버 재시작에도 등록 목록 유지), 자신이 등록한 아이템은 구매 불가, 취소하면 아이템이 그대로 인벤토리로 돌아옴
- 실기 검증: 계정 A가 상점에서 산 검을 거래장터에 30G로 등록 → 계정 B가 같은 검을 구매 → B의 골드가 정확히 30G 차감되고 인벤토리에 검이 들어오는 것 확인, 동시에 A가 온라인 상태로 즉시 30G를 받는 것까지 실시간으로 확인

### 검 6종 추가 (요청 사양대로)
- 상점(대장간 아님, 기존 무기 카탈로그)에 실버 소드(50G·공격+2) · 골드 소드(100G·공격+4) · 다이아 소드(150G·공격+6) · 레디언트 소드(250G·공격+9) · 바람의 숨결(350G·공격+12) · 마스터리 소드(500G·공격+16) 추가 — 기존 화염검/빙결검/강철검과 자연스럽게 이어지는 성장 곡선을 이루도록 공격력을 배치
- 실기 검증: 상점에서 6종 전부 정확한 이름·가격으로 노출되는 것 확인, 실버 소드 구매 후 골드가 정확히 차감되는 것까지 확인

### 튜토리얼
- 계정당 최초로 직업을 선택한 직후(또는 이미 직업이 있는 기존 계정이 처음 접속했을 때) **7단계 튜토리얼 팝업**이 한 번 뜸 — 이동/공격, 스킬, 성장, 장비·거래(거래장터 소개 포함), 퀘스트·길드, 레이드, 지도 순서로 안내. "건너뛰기"로 언제든 닫을 수 있고, 한 번 보면 계정에 영구 기록되어 다시 안 뜸(`user.hasSeenTutorial`)
- 실기 검증: 신규 계정으로 직업 선택 직후 튜토리얼이 뜨는 것, 7단계를 끝까지 넘기면 "시작하기"로 정상 종료되는 것 확인

## 24. 배포 준비 — 정적 파일+WS 통합 서빙, Supabase 영속성 계층, gh CLI로 GitHub 푸시 (v2.4, 완료)

### 깃허브 푸시
- gh CLI 설치 + 로그인(계정 WOOCHY338) 완료, `pixelrealm` 저장소를 **공개(public)**로 생성해 전체 커밋 푸시 완료: https://github.com/WOOCHY338/pixelrealm
- `.gitignore`에 실행 중 생성되는 계정/길드/거래장터 데이터 파일(`users.json`, `guilds.json`, `market.json`, `accounts.json`)과 `node_modules`는 계속 제외

### 정적 파일(클라이언트) + WebSocket을 한 포트로 통합
- 기존엔 클라이언트(정적 HTML/JS)는 Windows 전용 PowerShell 스크립트(`serve.ps1`)로 8765번, 게임 서버(WS)는 Node로 8766번 — 두 개의 서로 다른 프로세스/포트로 나뉘어 있었음. Render 같은 호스팅은 서비스 하나당 포트 하나만 열어주므로 이 구조로는 배포가 안 됨
- `server/index.js`가 이제 Node의 `http` 서버로 `client/` 폴더를 직접 서빙하고, 같은 `http.Server`에 WebSocketServer를 붙여서(`{ server: httpServer }`) **정적 파일과 실시간 통신을 완전히 한 포트에서 처리**하도록 변경(로컬 기본 포트는 8765, 배포 환경에서는 `PORT` 환경변수를 그대로 따름)
- `serve.ps1`은 이제 안 쓰여서 삭제, `.claude/launch.json`의 `pixelrealm-client` 설정도 통합 서버를 직접 실행하도록 변경
- 클라이언트(`game.js`)의 WebSocket 접속 주소도 `location.protocol`/`location.port`를 그대로 따라가도록 바꿔서, 로컬이든 배포 환경(https)이든 페이지를 불러온 곳과 항상 같은 곳으로 자동 접속(수동으로 주소 안 바꿔도 됨)
- 실기 검증: 통합 서버 기동 후 `http://localhost:8765/`에서 정적 파일·로그인·이동·거래장터 데이터(이전 세션에 등록한 골드 30G 등)까지 전부 정상 확인 — 리팩터링 후에도 기존 동작이 그대로 유지됨을 확인

### Supabase 영속성 계층 (선택적 — 환경변수 없으면 기존 JSON 파일 그대로 동작)
- `server/db.js`를 새로 만들어 계정/길드/거래장터 저장을 하나의 인터페이스(`loadUsers/saveUsers/loadGuilds/saveGuilds/loadMarket/saveMarket`)로 감쌈. **`SUPABASE_URL`과 `SUPABASE_SERVICE_KEY` 환경변수가 있으면 Supabase(Postgres)를 쓰고, 없으면 지금처럼 로컬 `users.json`/`guilds.json`/`market.json`으로 그대로 동작** — 로컬 개발은 Supabase 없이도 아무 변경 없이 계속 가능
- `supabase/schema.sql`에 테이블 스키마(users/guilds/market_listings) 정리해둠 — Supabase 대시보드 SQL Editor에서 그대로 실행하면 끝
- 서버는 `service_role` 키(비밀키, 절대 클라이언트/공개 저장소에 노출 금지)로만 접속하므로 RLS는 별도로 켜지 않음
- 실기 검증: 이번엔 환경변수를 아직 설정하지 않아 **JSON 파일 경로만 라이브로 확인**함(회귀 없음 확인). Supabase 경로 자체는 사용자가 프로젝트를 만들고 URL/service_role 키를 전달해주면 이어서 연결하고 검증할 예정

### Render 배포 준비
- `render.yaml` 블루프린트 추가(`rootDir: server`, `npm install` → `node index.js`, `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`는 대시보드에서 직접 입력하도록 `sync: false`로 비워둠) — Render에서 이 저장소를 연결하면 대부분 자동으로 설정됨
- 실제 배포(Render 계정 생성·저장소 연결·환경변수 입력)는 사용자가 직접 진행해야 하는 부분이라 아직 미완료

### 의도적으로 이번에 하지 않은 것
- Supabase 프로젝트 생성과 URL/service_role 키 전달은 사용자 몫 — 받는 대로 실제 Supabase 경로 연결해서 라이브 검증 예정
- Render 배포 자체(계정 생성 필요)는 사용자가 진행, 필요하면 이어서 도와드릴 예정

## 25. Render 실배포 완료 (v2.5, 완료)

- 사용자가 Render 계정 생성 후, Render CLI 설치·로그인(디바이스 코드 방식) → `render services create`로 `pixelrealm` 웹 서비스 생성 → 첫 배포까지 전부 완료
- **실제 플레이 가능한 주소: https://pixelrealm-1z77.onrender.com**
- 루트 디렉터리 `server`, 빌드 `npm install`, 시작 `node index.js`로 설정(로컬과 동일 명령), GitHub `master` 브랜치에 푸시할 때마다 자동 재배포(`autoDeploy: yes`)
- 실기 검증: 배포된 주소에서 정적 파일 로딩 → 회원가입 → WebSocket 연결(wss://) → 직업 선택까지 실제 브라우저로 확인 완료

### ⚠️ 중요 — 아직 남은 제약
- 지금은 Supabase를 아직 연결 안 해서 **Render의 로컬 파일(users.json 등)에 저장 중인데, Render 무료 플랜은 재배포할 때마다 디스크가 초기화됨** — 즉 지금 이 주소에서 만든 계정은 다음 코드 푸시(재배포) 때 전부 사라짐. Supabase 프로젝트 URL/service_role 키를 넘겨주시면 영구 저장소로 바로 전환 예정

## 26. Supabase 연결 완료 — 영구 저장소 전환 (v2.6, 완료)

- moneyroad 조직 아래 새 Supabase 프로젝트 `pixelrealm` 생성(Northeast Asia/Seoul 리전), `supabase/schema.sql`을 SQL Editor에서 실행해 users/guilds/market_listings 테이블 생성
- Render 서비스에 `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` 환경변수 등록 후 재배포 — 이제 계정/길드/거래장터 데이터가 **Render 재배포와 무관하게 영구 보존**됨(더 이상 파일 초기화 문제 없음)
- 실기 검증: 배포 주소(https://pixelrealm-1z77.onrender.com)에서 신규 회원가입 → Supabase `users` 테이블에 해당 행이 실제로 생성되는 것까지 SQL로 직접 확인 완료
- 서버 로그에서 `[db] Supabase 연결 사용`만 뜨고 로드 실패 메시지가 없는 것 확인 — JSON 파일 폴백이 아니라 실제 Supabase 경로로 동작 중

### 참고 — 키/시크릿 취급
- service_role 키는 채팅에 노출하지 않고 브라우저 클립보드 복사→붙여넣기로만 전달(에이전트가 직접 읽지 않도록 보호장치 적용)

## 27. 모바일 지원 (v2.7, 완료)

### 터치 컨트롤
- 터치 기기 감지(`ontouchstart`/`maxTouchPoints`) 시에만 화면에 가상 컨트롤이 나타남 — 데스크톱은 기존 키보드/마우스 그대로
- 왼쪽 하단 가상 조이스틱: 드래그 방향을 기존 WASD 로직(up/down/left/right)과 동일한 8방향 입력으로 변환, 놓으면 정지
- 오른쪽 하단: 공격 버튼(누르고 있으면 연타), 스킬 E·R 버튼
- 우상단: 인벤토리·스텟·지도·채팅 빠른 버튼(각각 B·C·M·Enter 키와 동일 동작)
- 마우스가 없는 터치 기기에서는 캐릭터 방향(조준)이 조이스틱으로 이동한 마지막 방향을 그대로 따라감(마우스 커서 기반 조준 대신)

### 반응형 레이아웃
- 상점·인벤토리·스텟·스킬상점·길드회관·거래장터·로그인·튜토리얼 등 모든 패널 너비를 `min(고정값, 92vw)`로 바꿔 좁은 화면에서 잘리지 않게 함(필요시 세로 스크롤도 추가)
- 뷰포트 메타 태그에 `maximum-scale=1, user-scalable=no`, `touch-action:none`, `overscroll-behavior:none` 적용 — 핀치줌·페이지 바운스·스크롤로 인한 조작 방해 방지
- 좁은 화면(≤640px)에서는 하단 HUD의 키보드 안내 문구(이동:WASD 등)를 숨겨 공간 확보
- `orientationchange` 이벤트에도 캔버스 리사이즈 재계산하도록 보강

### 실기 검증
- 브라우저 모바일 뷰포트(375×812)로 로그인→회원가입→직업선택→튜토리얼→인게임까지 전체 흐름 확인
- 합성 터치 이벤트로 조이스틱 드래그 시 방향 입력이 정확히 반영되는 것, 뗐을 때 정지하는 것, 공격 버튼이 실제 attack 메시지를 보내는 것, 인벤토리 버튼이 패널을 여는 것까지 개별 확인
- 인벤토리 패널 등 주요 패널이 375px 폭에서 잘리지 않고 전부 표시되는 것 확인

### ⚠️ 배포 관련 발견 — 자동 배포가 실제로는 동작하지 않음
- `render services create`로 CLI를 통해 만든 서비스는 `autoDeploy: yes`로 표시되지만, GitHub 웹훅이 실제로 연결되어 있지 않아서 `git push`만으로는 재배포가 안 됨(대시보드에서 "Connect repository" 절차를 거쳐야 웹훅이 붙는 것으로 보임)
- 그래서 이번 배치는 푸시 후 `render deploys create`로 수동 트리거해서야 반영됨 — **앞으로 코드를 바꿀 때마다 푸시 후 수동 배포 트리거가 필요**(또는 사용자가 Render 대시보드에서 저장소를 다시 연결하면 자동 배포가 정상화될 수 있음, 다음에 시간 날 때 확인 필요)
